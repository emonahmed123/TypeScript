/// noarmal fuction
// function add(num1: number, num2: number) {
//   return num1 + num2;
// }

// console.log("function");

const myFriends = [""];

function add(num1: number, num2: number): number {
  return num1 + num2;
}

console.log(add(2, 3));

const allAdd = (num1: number, num2: number): number => {
  return num1 + num2;
};

console.log(allAdd(2, 3));
